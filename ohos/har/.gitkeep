# This directory receives the HAR artifacts `flutter build hap` copies in:
# flutter.har (the engine embedding) and, when plugins with ohos support are
# added, one <plugin>.har per plugin. Nothing here is checked in.
